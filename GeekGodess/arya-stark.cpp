#include <iostream>
#include <vector>

using namespace std;

int main() {
    int t, r, p, count;
    cin >> t;
    while(t--) {
        cin >> r >> p;;
        
        count = 0;
        for (int i = p; i <= r; i++) {
            int arr[2] = {0,0};
            for (int j = 0; j < r; j++) {
                if 
            }
        }
    }
}
